(function() {
    'use strict';

    angular
        .module('newscache.route', [
            'newscache.route.interest',
            'newscache.route.login',
            'newscache.route.home'
        ]);
}());
